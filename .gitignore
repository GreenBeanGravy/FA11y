################################################################################
# This .gitignore file was automatically created by Microsoft(R) Visual Studio.
################################################################################

# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.so

# Visual Studio
/.vs/FA11y/v17
/.vs/FA11y/FileContentIndex
/.vs

# FA11y specific
/config.txt
/config/
/logs/
/debug

# User-local slug-migration backups (written by _migrate_current_map_slug
# the first time a legacy config is upgraded; kept on disk so the user can
# roll back to an older FA11y build by restoring them)
*.premigration

# Claude Code session state (local only)
/.claude/

# Stray debug captures
/debug/

# Per-install marker — written once after PowerShell Unblock-File so
# subsequent launches can skip the DLL unblock dance.
lib/mouse_passthrough/FakerInputWrapper.dll.unblocked